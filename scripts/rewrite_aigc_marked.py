from copy import deepcopy
from pathlib import Path
import re

from docx import Document


SOURCE = Path(r"C:\Users\Dou-ChengHao\Desktop\7230264110_窦成皓_23软件1_基于多智能体协同与大语言模型的多专科医疗健康咨询及辅助分诊系统.docx")
OUTPUT = Path(r"D:\毕设制作\outputs\7230264110_窦成皓_23软件1_基于多智能体协同与大语言模型的多专科医疗健康咨询及辅助分诊系统_AIGC标记修改版.docx")

# Paragraphs aligned to the color-marked text in the supplied PaperPass report.
MARKED = {
    47, 48, 120, 121, 123, 124, 127, 128, 129, 131, 132, 133, 140, 141, 142,
    150, 152, 156, 159, 163, 168, 170, 174, 176, 178, 179, 180, 181,
    192, 197, 198, 206, 211, 212, 217, 218, 219, 222, 225, 228, 231, 234,
    237, 240, 243, 246, 250, 251, 255, 256, 257, 262, 264, 270, 273, 277,
    278, 279, 280, 288, 289, 293, 304, 309, 310, 311, 312, 321, 322, 324,
    325, 326, 333, 334, 335, 336, 343, 344, 347, 348, 357, 359, 360, 362,
    363, 365, 366, 377, 378, 379, 384, 385, 396, 398, 399, 401, 413, 414,
    426, 427, 432, 433, 437, 441, 450, 451, 452, 453, 454, 461, 464, 465,
    470, 471, 476, 477, 483, 491, 496, 497, 499, 502, 503, 506, 507, 509,
    512, 515, 516, 518, 519, 525, 527, 528, 531, 533, 535, 536, 537, 538,
    540, 543, 545, 550, 551, 552, 553, 554, 555, 557, 558, 559, 560,
    614, 615, 616, 617, 618, 619,
}


MANUAL = {
    47: "在线健康咨询里，用户往往先用口语描述不适，症状、持续时间和严重程度并不总是一次说全。系统既要把这些描述整理成可处理的信息，也要先判断其中是否存在影响就医时效的危险表现。若直接让大语言模型生成建议，处理顺序和依据都不容易固定下来。基于这一考虑，本文实现了多专科医疗健康咨询及辅助分诊系统 MedPilot。系统不作疾病诊断，主要完成症状整理、风险提示和就医科室推荐，为用户提供辅助参考。",
    48: "MedPilot的前后端分别使用 Vue 3、Spring Boot 和 FastAPI，问诊流程由 LangGraph 负责组织。一次咨询先经过危险信号筛查，再进行症状抽取和信息充分性判断：发现危险信号便转入快速路径；信息不全时先追问缺失字段；达到当前分诊条件后，才用 bge-m3 生成查询向量，并结合 FAISS 检索和词法重合度获取医学证据，进而分析风险等级与科室方向。系统还保存证据快照、节点事件和处理终态，并提供问诊记录、授权健康档案、医生复核、知识审核、索引版本管理、Trace监控和审计功能。",
    120: "本研究的工作重点是把自然语言问诊、分工明确的多智能体流程、医学知识检索和安全治理落到一套可以运行的软件中。系统要完成症状采集、危险信号筛查、信息充分性判断、主动追问、证据检索、辅助分诊和结果展示；同时，每轮处理都应能回看到具体流程阶段及其使用的知识来源。",
    121: "实现时，本文没有采用多个模型自由对话的方式，而是给每项职责设置固定节点，并约定结构化的输入和输出。危险信号筛查放在模型推理之前；信息不足就转入追问；证据达到条件后再给出科室建议。认证、持久化和审计由业务后端承担，医生复核与知识治理作为后续人工控制环节。",
    123: "从实际使用看，系统先把用户的口语描述整理成较有条理的问诊信息，再在结果页并列显示风险提示、就医时效、建议科室和参考来源。用户看到的不是一段孤立的生成文本，而是一条可以按步骤理解的咨询流程。",
    124: "从工程实现看，条件工作流把安全筛查、信息采集、检索和回答编排拆成可以分别测试的节点；知识审核、版本管理、Trace和审计则负责留下检查线索。已有多智能体医疗系统研究也指出，工具调用、流程衔接与安全限制需要在同一架构中安排[1]。",
    127: "Hager团队在2024年用2400个真实病例和4类常见腹部疾病搭建了临床流程模拟，指出医学考试成绩不能直接当作真实决策流程的替代评价[2]。同年，Tam团队把医疗大语言模型的人类评价分成信息质量、理解与推理、表达与角色、安全与伤害、信任与信心5个维度，为系统级评价提供了更细的观察框架[3]。",
    128: "Chen团队在2025年以302个罕见病病例比较单模型与多智能体会话框架，表现较好的配置包含4个医生智能体和1个监督智能体，显示角色分工可以用于组织复杂医学任务[4]。Gaber团队则在2000个MIMIC病例上考察分诊、转诊和诊断工作流，并把检索增强放进统一的决策支持流程[5]。",
    129: "Kang团队于2026年利用6624份三级医院转诊信研究本地大模型分诊，在680份留出集上得到75.4%的基线一致率；对不一致样本进行专家裁决后，该比例为84.7%，另有5.9%的材料无法唯一分配专科[6]。Flanders团队分析29766份影像报告，比较8个开源模型、GPT-4o和模型共识，并用1490份人工复核报告评价多模型组合[7]。这些工作关注的重点，已经从单次问答成绩延伸到真实流程、人工复核和运行治理。",
    131: "国内相关工作在2024—2026年主要沿着模型适配、门诊分导诊、在线问诊理解和医学检索增强展开。孙丽萍团队在2024年利用医疗临床数据完成两阶段专业级大语言模型微调，为领域适配提供了实现思路[8]；杨霞团队在2025年把分诊准确率和患者满意度一起纳入人工智能门诊分导诊评价，评价对象由算法输出扩展到服务效果[9]。",
    132: "关于医疗智能助手的2025年综述，归纳了文本处理、问答和辅助决策等应用方向[10]。2026年，张建同团队从医患交流文本出发研究在线问诊主题识别，处理的是自然语言入口的信息理解问题[11]；同年，公共卫生智能问答研究把检索增强用于具体健康服务[12]，慢性病问答研究则用 GraphRAG 与混合专家模型组织多源知识[13]。",
    133: "上述研究为本文的模型应用、分诊评价、检索增强和多智能体协同提供了参考。结合本科毕业设计的实现边界，MedPilot把这些方向收束为一条固定的条件工作流，并将权限、复核、知识版本和运行追踪放进同一系统范围。",
    140: "本章先交代医疗多智能体辅助分诊系统的背景，再说明研究目的和工程意义。公众健康咨询需求增加后，线上问诊在信息收集、专业分工和结果解释方面都提出了更高要求；大语言模型的理解与生成能力，为改善交互、整理患者信息和支持初步分诊提供了技术条件。基于这些问题，本文明确系统要解决的任务，并说明其在咨询效率、流程规范和结果追溯方面的应用价值。",
    141: "从2024—2026年的国内外研究可以看到，医疗大语言模型的应用正在从单一问答转向流程化、证据化和可治理的系统。研究者开始同时考察多阶段协同、知识支撑、风险控制、过程记录和人工复核，而不再只比较一段医学回答的生成效果。对系统设计而言，这意味着语言能力、业务流程和安全治理需要一起考虑，通用模型本身并不等同于完整的医疗服务系统。",
    142: "因此，本文把 MedPilot 定位为面向多专科健康咨询的辅助分诊系统。它按既定流程组织信息采集、症状分析、风险判断、科室建议和结果复核，为用户提供结构化咨询与就诊引导。系统输出只作辅助参考，不替代医生的诊断和治疗决定；这一边界也构成后续需求分析、架构设计、实现和测试的前提。",
    150: "技术方案由 Vue 3、Spring Boot、FastAPI、MySQL、Ollama 和 FAISS 组成。Vue 3负责交互与状态呈现，Spring Boot处理认证、业务规则和持久化，FastAPI承载多智能体工作流，本地模型与向量索引分别承担语言理解和知识检索。各服务通过 REST 与 NDJSON 连接，现有代码、测试和部署脚本已覆盖主要业务链路，因而具备落地条件。",
    152: "项目主要依靠开源框架和本地开发环境，不需要专用商业接口。普通开发设备或实验室服务器即可承担模型推理，数据库、前端和后端也能用现有工具部署。成本主要来自开发时间、测试数据整理和后续维护，处在本科毕业设计可承受的范围内。",
    156: "系统在功能定位上明确不输出确诊或处方。身份认证、角色授权、医疗关系校验、加密和审计共同保护敏感数据；知识文档进入检索集合前还要经过审核。就健康信息服务、就医引导和医疗安全治理而言，这一定位与实际需求相符。",
    159: "对普通用户而言，系统提供登录、症状描述、附件上传、追问回复、分诊结果查看、历史记录查询、健康档案维护、复诊任务管理和健康知识检索。用户需要了解的也不只是推荐科室，还包括风险提示、就医时效、判断依据以及可以查看的知识来源。",
    163: "问诊过程中，系统应持续返回节点状态和回答片段，让用户知道处理仍在进行；流式事件要按递增序号到达，并以明确的终态结束。记录查询和管理列表需要支持筛选，索引切换不能影响正在使用的版本，请求异常结束后还要释放连接和运行状态。",
    168: "LangGraph把带有状态和条件分支的处理过程表示成有向图。本文所说的“多智能体”，是指一组职责固定、输入输出契约独立且会产生可观测事件的流程节点：安全筛查负责识别危险信号，症状节点负责结构化抽取，追问节点负责补全信息，检索节点负责召回证据，分诊节点进行规则化投票，回答节点完成确定性编排。节点结果写入共享状态，条件边据此选择下一步；这些节点不是可以脱离系统独立诊断的自治医生。采用这样的定义后，每个节点都能单独测试、替换和回溯，也与多智能体会话研究强调的职责分工相符。",
    170: "FastAPI对外提供AI服务接口，Pydantic则约束输入、节点状态、证据、分诊结果和事件载荷。模型输出进入业务流程前，先按结构化契约解析并校验；Spring Boot、前端和测试由此使用同一套数据定义。",
    174: "FAISS保存医学知识切片的向量，并以归一化内积完成检索；向量归一化后，该分数可以看作余弦相似度。设向量分数为 S_dense，中文字符二元组重合度为 S_lex，综合分取 S=0.8S_dense+0.2S_lex；候选池为 min(4K，N)，其中 K=3，N 是索引分片数，过滤条件为 S≥0.35，最后保留综合分最高的 Top-3。这里把语义分数作为主项，同时给短症状的字面命中留出权重；0.35 和 Top-3 只是当前 Demo 在噪声、页面证据数量与响应开销之间采用的工程参数，并非全局最优值。检索增强研究指出，外部证据能进入生成流程的前提是检索模块和生成模块之间有清晰接口[14]；医疗服务调度研究也提示，优先级与资源分配需要在流程中明确表达[15]。医学 RAG 综述进一步指出，文档切分、查询表达和候选重排都会影响检索结果[16]。",
    176: "NDJSON把每个节点事件写成一行 JSON，HTTP 响应可以据此连续返回安全筛查、症状抽取、追问、检索、分诊、回答片段和终态。Trace则用标识和序号把同一次咨询的事件串起来，前端据此显示进度，后端据此判断持久化条件，管理端也能沿调用链定位问题。",
    178: "本章从技术、经济、操作和社会四个角度检查 MedPilot 是否具备建设条件。现有开发环境、项目投入、用户操作方式和应用场景共同构成判断依据。技术部分关注模型、工作流、知识检索和服务端开发能否协同；经济部分核对开发与运行成本；操作部分考察业务流程是否容易使用；社会部分则回到健康咨询和就医引导的实际需求。",
    179: "需求分析把系统要服务的对象和质量边界进一步落了下来。用户需求对应主要使用者及其目标，功能需求覆盖健康咨询、多轮问诊、辅助分诊、健康管理、知识维护和临床复核，性能需求关注响应、稳定性和连续性，安全需求则约束身份、权限、健康数据及关键过程记录。这些内容一起成为后续设计和验收的依据。",
    180: "本章还梳理了系统依赖的技术。多智能体工作流负责安排任务节点，本地模型提供可控的调用方式，混合检索连接医学知识与问诊证据，事件追踪保存流程状态和处理结果。把这些技术与业务需求逐项对应，才能说明它们在系统中的具体作用。",
    181: "综合来看，本章完成了从建设条件到功能范围的过渡：可行性分析回答“能不能做”，需求分析回答“要做什么”，技术基础说明“怎样落地”。后续的总体设计、数据库设计、模块实现和测试都以此为起点。",
    192: "在图3-1中，系统功能被分成账号与身份认证、智能问诊、危险信号筛查、主动追问、医学知识检索、辅助分诊、问诊记录、健康档案、医生复核、知识治理、运行监控和审计管理。前六项组成咨询主链路，其余功能负责数据留存、人工介入和持续治理。",
    197: "管理员端可以查看咨询量和风险分布，维护知识文档及索引版本，处理复核和模型治理任务，查看 Trace 与异常状态，并管理用户、角色和审计记录。每项操作都由后端角色规则再次校验。",
    198: "用户端的功能主线是账号访问、智能问诊、分诊结果、问诊记录、健康管理和知识服务，整体结构见图3-3。",
    206: "数据关系以用户为起点：一个用户可以建立多个问诊会话，每个会话包含多条消息并形成一条问诊记录；记录再关联流程事件、临床复核任务和引用的知识文档。健康档案与用户是一对一关系，审计日志则保存受保护资源的操作结果。",
    211: "用户用例中，智能问诊包括症状填写、追问回复和结果查看；问诊记录支持筛选、详情和证据链查看；健康档案包含档案维护与复诊任务；知识服务提供健康知识检索和常见问题查看。",
    212: "管理员用例主要围绕用户权限、知识库、复核治理以及监控审计展开，角色之间的关系见图3-6。",
    217: "本文从项目Java源码中挑选了10个与用户问诊主流程关系最直接的实体类。它们覆盖账号、患者、在线问诊、健康管理、医学知识库和临床复核等环节，用来说明对象如何从用户进入平台、提交信息，一直组织到结果生成和人工复核。",
    218: "类图由 IntelliJ IDEA 根据实际Java源码导出，保留实体类的主要字段、构造方法和业务方法。为便于和数据库结构互相核对，各实体按业务链路排列，并在文字中注明对应数据表。这样既能看到对象模型，也能追踪其持久化位置。",
    219: "在账号与权限模块中，User类保存用户账号、身份来源、角色和组织范围。它既承载平台身份，也为问诊访问、数据授权和业务范围控制提供依据。图3-7展示了源码识别出的字段、构造方法和业务方法；持久化层将该实体映射到 users 表，便于从业务对象追到具体存储。",
    222: "Patient类记录医院主患者索引及其映射关系。它与侧重平台账号的 User类不同，关注的是患者在医疗业务中的身份，因此会被问诊记录、健康档案和复诊任务共同引用。图3-8给出了该类的字段和方法，数据库中的对应表为 patients。",
    225: "ConsultationSession类把一轮连续问诊组织成一个会话，主要保存归属信息和最近活动时间。消息、流程事件和问诊结果都以它作为共同上下文，最近活动时间还可用于会话查询和后续管理。图3-9为该类源码结构，数据库映射表是 consultation_sessions。",
    228: "ConsultationRecord类沉淀一次问诊结束后的结构化结果，包括问诊结论、风险等级、科室建议和证据快照。ConsultationSession记录过程，而 ConsultationRecord 更关注完成后的结果及其后续使用。图3-10同时展示了该类的方法结构和与 consultation_records 表的对应关系。",
    231: "ConsultationMessage类保存多轮问诊中的用户输入和系统回复，并按会话归档交互内容。消息持久化后，可以重现对话，也能为结果查看和临床复核提供依据。图3-11展示了该类的字段和方法，数据存放在 consultation_messages 表。",
    234: "ConsultationTrace类记录问诊流程内部的事件和状态变化。它与 ConsultationMessage 的职责不同：前者关注安全筛查、症状抽取、检索、分诊等节点的执行情况，以及最终状态和耗时。图3-12给出了源码结构，数据库表为 consultation_traces。",
    237: "HealthProfile类承载用户授权使用的健康背景，如过敏史、既往情况和用药提示。该对象服务于问诊前后的健康资料管理，只有在授权范围内才会被读取。图3-13展示其字段和方法，对应的持久化表是 health_profiles。",
    240: "FollowUpTask类把问诊后的复诊和随访安排保存成任务，记录任务类型、到期时间和处理状态。借助状态字段，系统可以区分未处理、处理中和已完成事项。图3-14为该类结构，对应表为 follow_up_tasks。",
    243: "KnowledgeDocument类管理医学知识文档及其处理状态，涵盖文档内容、审核状态和向量化状态，反映知识从录入、审核到建立索引的过程。它为问诊流程提供知识资源，但职责不同于问诊业务实体。图3-15展示了该类，数据库对象为 knowledge_documents。",
    246: "ClinicalReview类连接自动生成的问诊结果和医生复核流程，保存复核任务、处理决定及人工结果。确认、修改、退回或升级等决定单独记录，既能保留人工介入过程，也方便追踪责任。图3-16展示了该实体，数据库表为 clinical_reviews。",
    250: "业务数据存放在 MySQL，Flyway按版本创建和扩展表结构。本节沿着“身份确认—患者映射—会话交互—结果复核”的主链路，选取10张核心表说明。选择标准包括对象与表的一致映射、主链路的直接引用和结果追溯所需的最小字段；模型发布、红队测试及回滚演练等治理表仍由迁移脚本维护，但不在本节逐表展开。表中的“约束”涵盖主键、非空、自增、唯一、外键和默认值，归属字段用于限制跨用户和跨会话写入。",
    251: "users表是认证授权的根表，保存账号、身份来源和角色。问诊会话、审计日志等对象通过其主键建立引用，角色与 active 状态共同决定用户可以访问的功能。字段见表3-1。",
    255: "consultation_sessions表描述一轮连续问诊，保存所属用户、会话状态和最近活动时间。消息与问诊结果以此获得稳定的归属边界，便于恢复会话并隔离不同用户的数据。字段见表3-3。",
    256: "表3-3 问诊会话表（consultation_sessions）",
    257: "consultation_records表沉淀一次问诊产生的结构化结果，包括风险等级、建议科室、就医时效、回答文本和证据快照。结果页面、历史记录和临床复核都从该表读取主要数据。字段见表3-4。",
    262: "consultation_messages表按顺序保存用户输入、系统追问和最终回答，并通过会话外键关联 consultation_sessions。消息序号使多轮对话可以按原顺序重放。字段见表3-5。",
    264: "consultation_traces表记录危险信号筛查、症状抽取、检索、分诊和回答编排等节点事件。它与问诊记录关联，既支持前端展示进度，也为异常定位和审计回溯留下线索。字段见表3-6。",
    270: "knowledge_documents表登记医学知识文档，集中记录审核结果、版本信息和索引状态。只有审核通过的文档才会进入普通检索集合，问诊引用可通过文档标识回到来源，核对证据出处和版本，并支持后续维护。字段及含义见表3-9。",
    273: "clinical_reviews表保存医生复核辅助分诊建议时的处理信息，包括领取、确认、修改、退回和升级决定，以及处理人员和时间。复核记录与 AI 原始结果分开存储，人工操作不会覆盖原始输出，同时便于追踪责任和统计治理结果。字段及含义见表3-10。",
    277: "本章把 MedPilot 的概要设计拆成四个层面：功能结构、角色边界、数据组织和对象模型。系统先按业务目标梳理管理员端与用户端，再用角色范围约束模块职责。管理员端处理运行维护、知识资源和临床复核，用户端围绕健康咨询、问诊交互、结果查看和后续管理展开。",
    278: "E-R关系和角色用例分别从数据和参与者角度补充业务模型。前者说明用户、患者、会话、记录、档案、复诊任务、知识文档和临床复核之间怎样关联，后者说明用户与管理员从哪里进入这些功能、各自能做什么。两者结合后，概要设计不再只是模块名称的罗列。",
    279: "对象模型部分选取10个核心实体类，持久化模型部分选取10张核心业务表。实体类图来自 IntelliJ IDEA 对实际Java源码的分析，数据库结构依据 Flyway 迁移脚本和实体映射整理。实体与表的一一对应，建立了业务对象、Java实现和数据库存储之间的追踪关系。",
    280: "至此，系统的功能范围、角色关系、数据关联以及对象和表的对应关系已经确定。下一章将在这一设计基础上展开模块实现、接口处理、数据库部署和测试对象说明。",
    288: "登录接口由后端 `AuthController` 提供。请求先做参数校验，再按用户名查询用户并用 BCrypt 校验密码；账户启用后签发 JWT，通过 HttpOnly Cookie 返回浏览器。前端只保存登录状态，不接触 AI 服务令牌。`/api/auth/me` 用于读取当前用户，退出时借助令牌版本和 Cookie 清理使会话失效。",
    289: "业务请求进入控制器前，会先经过 `JwtAuthFilter`。过滤器解析 JWT，提取用户名和角色，随后建立 Spring Security 认证对象。角色字段即使由前端提交也不会被信任，资源访问仍由服务端授权规则决定；令牌缺失、签名错误、账户不存在或账户失效时，请求直接返回未认证状态。",
    293: "`SecurityConfig`按照请求路径和 HTTP 方法划定角色边界。DOCTOR 与 REVIEWER 可以访问临床复核接口；ADMIN 或 KNOWLEDGE_EDITOR 负责知识文档上传、入库和版本构建；知识审核、模型评测审核和模型回滚开放给 ADMIN、REVIEWER 或 DOCTOR；模型冻结和治理变更仅由 ADMIN 执行。审计、监控读取权限再分别授予 ADMIN、AUDITOR 及规定的复核角色。",
    304: "敏感字符串和附件流由 `DataEncryptionService` 使用 AES-256-GCM 保护。活动密钥解码后必须是32字节；每次加密随机生成12字节 Nonce，并用128位认证标签检查密文完整性。字符串密文带有 `enc:v1` 和密钥标识，附件则以独立文件前缀保存密钥标识与 Nonce。",
    309: "AI服务 `run_consult_stream` 一开始就执行危险信号筛查。规则词表由项目内的 DANGER_SIGN_TERMS 和 _ALIASES 维护，包含胸痛、呼吸困难、气促、咯血、便血、意识不清、晕厥和大出血，以及气短、黑便、昏倒、咳血、咳出血和咳出了血等已登记同义词。它只用于控制软件路径，不等同于临床指南或诊断标准。`match_danger_signs` 先按标点、换行和转折词切句，再检查词项前是否出现“没有、否认、未见、无”等否定表达，并依据连接词继承否定状态；只有被断言出现的规范词才会触发高风险路径。",
    310: "筛查器先判断信号是否处在否定表达中，再处理连接词带来的范围。例如，“没有胸痛，也没有呼吸困难”不会命中，而“没有胸痛，但出现呼吸困难”会命中后一个信号。处理结果被封装成 `SafetyScreenResult`，其中保存命中状态、命中项、风险路径和规则标识。",
    311: "一旦发现危险信号，`classify` 直接采用高风险规则，生成风险等级为“高”的 `TriageResult`。随后返回安全筛查、辅助分诊、回答编排和 done 事件；确定性模板会列出危险信号、规则名称和就医时效。此路径跳过普通抽取、向量检索和自由回答生成，降低模型或索引故障对高风险提示的影响。",
    312: "危险信号处理的核心不是词表本身，而是先区分“出现”与“否认”。程序清单4-2给出了这种否定感知匹配的实现。",
    321: "没有命中危险信号时，流程才进入 extract 节点。提示词要求聊天模型只返回结构化 JSON，服务端由 `_parse_llm_json` 截取 JSON 边界后交给解析器；解析失败时返回空结构，不把自然语言直接塞进结构化字段。已有医疗文本研究讨论了主题和关键信息抽取[16-18]，这些工作共同说明文本理解需要结合任务结构与领域约束，因此本文继续用字段校验和规则复核限制模型输出。",
    322: "`normalize_symptoms` 会统一抽取结果，历史信息则转成不可变结构；当前文本还要再次经过危险信号匹配器。健康档案进入提示词前先做白名单和长度限制，并标注为“授权背景资料”，避免历史信息被当作本轮危险信号，也防止背景文本中的指令改变系统行为。",
    324: "信息充分性由 `is_sufficient` 这一独立规则判断，不依赖大语言模型。存在 red_flags 时直接走快速路径；结构化症状达到两个及以上可进入检索；只有一个症状时，至少还要有持续时间或严重程度。其余情况交给 `build_followup`。",
    325: "追问生成器按缺失字段的优先级提问：没有症状就先问主要不适；没有持续时间就问症状持续了多久；持续时间已有但症状仍少，就询问是否还有其他不适；其他情况再询问严重程度。`FollowUpQuestion` 返回问题文本和 missing 元组，前端依据 missing 字段显示等待补充状态。等待追问的会话不会被写成低风险分诊结果。",
    326: "程序清单4-3把这段规则拆开显示：先检查症状数量、持续时间和严重程度，再依据缺失字段的顺序生成一个问题。",
    333: "达到信息充分条件后，`_retrieve_node` 把原始文本与结构化症状合成查询。检索器先用 `expand_query_with_aliases` 扩展同义表达，再由 bge-m3 生成归一化向量，并在 FAISS 中取得 Top-3 的四倍候选。每个候选同时计算 S_dense 和 S_lex，按 S=0.8S_dense+0.2S_lex 融合；综合分低于0.35的候选被过滤，最后保留3条。向量权重0.8保证语义检索占主导，词法权重0.2保留短症状的直接命中，阈值和 K 值与2.3.4节一致，便于复现。检索增强研究指出，候选重排和证据组织会影响医学问答质量[16]；因此系统会保留审核通过的证据、来源文档和索引版本，再按科室分数加权。",
    334: "检索结果先与 `MEDPILOT_RAG_MIN_SCORE` 比较，低分项直接丢弃；剩余结果按综合分排序并截取 Top-K。每条记录封装为 `RankedEvidence`，同时保留来源定位、科室、证据原文、分数、索引版本和审核状态。",
    335: "`classify` 会再次检查高风险规则，然后只对呼吸内科、消化内科、心血管内科和皮肤科的证据计分。各科室累加证据分数，最高者成为推荐科室，再依据其占总分的比例计算支持分和置信度。没有有效证据时，系统返回全科/建议线下分诊台，风险等级设为低、abstained 设为真，并提示补充信息或转线下分诊。",
    336: "检索排序需要同时照顾医学语义和短中文症状的字面命中，程序清单4-4给出了向量分数与词法重合度的融合实现。",
    343: "`compose` 不会把聊天模型的自由回答直接写入系统。它先用 `_is_trusted_evidence` 检查证据的科室、审核状态、HTTPS URL、机构、题名、发布日期、版本和许可字段，再过滤含有确诊、处方、剂量或用药等受限内容的证据。Almanac 工作强调，临床回答应建立在可追溯外部证据上[19]；所以这里只渲染通过治理检查的引用，证据不足时改为低证据提示并提供人工复核入口。",
    344: "通过字段校验的 `TriageResult` 交给 `_render_answer` 确定性渲染，输出推荐科室、风险等级、就医时效和执业医生复核提示。若科室、风险或时效不在允许枚举内，就使用安全回退值；结果还附带安全边界声明，说明系统提供的是辅助分诊建议，不替代医生诊断和治疗。这种做法减少了语言自由度，但换来了字段可控和失败关闭。",
    347: "业务后端的 `ConsultController` 通过 WebClient 打开 AI 服务流，以 Reactor Flux 逐行接收。每行先由 `ConsultationEventAccumulator` 解析和校验，再发布到 `LiveTraceRegistry` 供监控页面订阅。只有收到 done 事件、累计器没有终态错误且记录条件满足时，后端才调用持久化服务；流提前结束则生成 INCOMPLETE_STREAM 错误。",
    348: "程序清单4-5展示了统一事件封装：事件发射器为每条记录写入协议版本、Trace、会话标识、递增序号、阶段状态和节点数据。前端进度和后端持久化由同一份事件信息驱动。",
    357: "问诊开始后，后端先由 `SessionOwnershipService` 校验并认领会话，避免不同用户交叉使用。随后保存本轮用户消息，再按用户和会话读取历史上下文；AI服务返回回答或追问后，后端追加助手消息。历史内容按用户、助手角色保存，后续轮次可以使用完整历史，也可以使用摘要上下文。",
    359: "`ConsultationPersistenceService` 在事务中写入 Trace、事件和问诊结果，并在写入前检查执行轨迹是否已有记录，防止重复提交产生重复数据。只有终态明确、业务结果完整时，科室、风险、置信度、支持度、解释、回答、引用和对话上下文才会一起保存。",
    360: "问诊记录写入成功后，如果临床复核已启用且还没有复核聚合，系统在同一事务内创建 `ClinicalReview`。AI原始结果在复核期间保持不变，医生决定存入独立字段；用户因此既能看到原始建议，也能在复核结束后看到最终科室、风险和时效。",
    362: "`HealthProfileController` 提供档案查询、更新和删除接口。更新时，系统保存用户授权的过敏史、既往情况、用药信息和备注，并记录是否同意将其用于问诊。读取始终依据当前认证身份，不能通过请求参数访问其他用户的档案。",
    363: "复诊任务由 `FollowUpTask` 与问诊记录关联，提供任务列表、单条查询、完成和状态更新。到期任务按当前用户和开放状态筛选，不会把其他用户的提醒混入列表。",
    365: "`RecordDetailView.vue` 把一条记录分成分诊概览、症状摘要、判定依据、系统建议、会话内容、记录信息以及依据与参考几个区域。页面显示 AI 推荐科室、AI 风险、支持度和就医时效；存在复核字段时，再单独显示医生最终科室、风险和时效。引用区保留来源、科室、证据原文、chunk_id 和 index_version，Trace ID 放在记录元数据中。",
    366: "记录详情依据 reviewStatus 显示等待复核、医生确认、医生调整、急诊人工流程或系统接管等状态，高风险记录另外显示紧急提示。页面把 AI 结果称作辅助分诊概览，并将医生决定与原始输出分开保存，避免用户把系统结果理解为临床诊断。",
    377: "知识库支持结构化文本和文件两种入库方式。上传时校验科室、机构、题名、来源链接、发布日期、版本和许可等字段，并按文件类型解析文本；同时限制文件大小、计算 SHA-256 摘要并保留来源。PDF 文本由 PDFBox 提取，失败时保留失败状态和原因。",
    378: "业务层把请求转给 AI 服务前，先执行 `sanitizeIngestPayload`，移除客户端不应提交的审核字段，强制新文档进入待审核状态，并重新计算摘要以防止元数据被改写。文档元数据存入 MySQL，切片和向量化结果由 AI 服务管理。",
    379: "文档进入 AI 服务前，后端会清洗审核字段并重新计算摘要，关键处理见程序清单4-6。",
    384: "文档审核通过后，审核接口通过 X-MedPilot-Reviewer 传递当前认证主体，同步更新 AI 服务中的审核结果、审核人和审核时间。知识版本接口负责建立新索引、切换活动版本并比较版本差异。TRIPOD-LLM 指南要求交代数据、任务、评价过程和适用边界[20]；本系统因此把审核结果、索引版本和变更记录作为知识治理证据保存。",
    385: "检索结果带有索引版本标识，引用快照可以还原本次咨询实际使用的知识环境。知识文档删除、审核、版本切换和索引操作都受角色权限约束；解析状态与向量状态分开记录，避免把“已入库”误解成“可供检索”。",
    396: "临床复核控制器提供记录详情、领取和决定接口。复核列表按状态和创建时间查询，领取动作同时检查版本与状态，防止多人处理同一任务；领取成功后，任务进入 IN_REVIEW 并记下处理时间。",
    398: "复核表单提供 CONFIRM、MODIFY、REJECT 和 ESCALATE 四种决定。CONFIRM 保留 AI 原始结果；MODIFY 要求提交最终科室、风险等级、就医时效和复核依据；REJECT 退回人工处理；ESCALATE 转入急诊人工流程。复核依据为必填项，接口还记录复核人、员工号、理由和处理时间。",
    399: "`ClinicalReviewView.vue` 在列表中显示待复核数量、状态筛选和原始分诊信息，选中任务后同时呈现 AI 建议与最终决定表单。高风险或急诊升级状态会显示院内流程提示，页面明确 AI 结果只是待复核草案；复核由此成为独立工作流，而不是直接修改一条 AI 记录。",
    401: "治理控制器覆盖模型发布、评测、回滚、知识来源登记、变更审批、红队测试、回滚演练、监控快照和安全事件。模型发布时要记录版本、签名、提示词与嵌入配置、知识索引版本、硬件基线及审批证据，高风险操作由具备相应权限的角色执行。算法偏见研究指出，人工智能决策需要配套的偏差治理[21]；因此，模型发布、评测、回滚和安全事件在本系统中都是可审计操作。",
    413: "`MonitorView.vue` 维护健康状态、历史 Trace、实时 Trace、节点状态和事件列表。页面从健康接口读取模型服务、知识索引和治理状态，用 Trace 列表查询历史运行，再通过事件流定位当前活动。节点列表对应安全筛查、症状采集、追问、知识检索、辅助分诊和回答编排。",
    414: "Trace详情按会话、阶段和轮次排列事件，并汇总节点耗时、失败原因、终态和引用数量，便于定位问题。监控图表只反映已经采集的运行样本，不能直接当作医疗质量指标。",
    426: "`AuditLogInterceptor` 为受保护的 Web 请求记录操作者、角色、HTTP 方法、动作、响应状态、请求标识、地址摘要和耗时，成功与失败结果都会写入审计日志。监控页和审计页分别提供查询、导出入口，角色只能读取权限范围内的记录。",
    427: "审计日志页可按操作者和响应状态筛选，主要显示时间、角色、动作、结果和耗时，页面见图4-11。",
    432: "后端把 AI 服务异常分为连接失败、HTTP错误、请求过大、会话冲突、容量超限、推理超时和流不完整等类型。首次事件前的可重试错误只做有限的指数退避；参数错误、冲突和超大请求不重复提交。流式处理中若客户端取消，后端保存取消 Trace 并释放上游连接；若没有收到 done 事件，累计器生成 INCOMPLETE_STREAM 错误。",
    433: "异常持久化遵循“先返回原始错误”的原则。数据库同时不可用时，系统不会让持久化失败遮住 AI 错误，而是尽量返回可操作的错误代码；`LiveTraceRegistry` 也会收到 failed 或 cancelled 状态，监控端不会把该请求一直留在运行中。",
    437: "`ConsultView.vue` 展示症状输入、历史消息、健康档案上下文、流式节点状态、追问、分诊结果、引用和安全边界。提交后先显示安全筛查，再随事件更新症状采集、知识检索、辅助分诊和回答编排状态。追问区标出缺失信息类别，用户补充后继续下一轮；高风险状态则在独立警示区显示就医时效。",
    441: "前端分别处理加载、空数据、错误、正常完成和风险升级状态。问诊页与记录详情在窄屏下由双栏改为单栏，引用、Trace ID和长文本采用换行避免溢出。AI推荐、医生最终决定和安全声明放在不同视觉区域，减少误读。",
    450: "本章按照代码和页面的对应关系，说明了 MedPilot 如何把用户输入处理成结构化辅助分诊结果。问诊链路先由否定感知匹配器筛查高风险症状，符合条件的请求进入快速通道；一般请求交给本地大语言模型，抽取症状、持续时间和严重程度。规则模块随后检查信息是否充分，决定是否追问。信息达到检索条件后，BGE-M3完成向量化，FAISS召回医学证据，确定性渲染器再生成辅助分诊说明。各模块按这一顺序衔接，形成从风险识别到结果输出的实现链路。",
    451: "除问诊流程外，业务后端还承担运行支撑。JWT负责身份认证，CSRF、角色授权和会话所有权校验限制访问范围和数据归属；事件累积记录处理过程，幂等持久化防止重复请求，异常处理则统一返回运行中的错误。问诊逻辑和这些基础能力结合后，服务端才能同时满足安全、数据一致和过程追踪要求。",
    452: "针对持续使用场景，本章还介绍了健康档案、医学知识库、医生复核、Trace监控、审计和治理。健康档案保存用户授权的背景信息，知识库管理医学文档和状态，医生复核为自动结果提供人工入口，Trace与审计记录关键运行事件。治理功能进一步规定知识维护、临床介入、过程监督和运行管理的边界，使系统可以支撑后续管理，而不只完成一次问诊。",
    453: "前端把后端返回的问诊状态、处理结果和检索证据分配到问诊、记录、知识、复核和监控等工作区。用户在问诊区提交信息并进行多轮交互，在记录区查看历史结果；管理员和相关工作人员通过其他工作区维护知识、处理复核任务并查看运行状态。统一的数据状态和业务接口负责连接这些页面。",
    454: "本章完成了从核心流程、业务后端到前端工作区的实现说明。问诊链路形成辅助分诊结果，后端基础设施处理安全、可靠性和追踪，健康管理与治理模块支持持续使用，前端则把状态和证据呈现给不同角色。下一章据此开展功能测试、性能分析和边界验证。",
    461: "测试环境包括前端、业务后端、AI服务、数据库、模型服务和向量索引。前端运行在 Node.js，后端使用 Java 17 和 Spring Boot，AI 服务使用 Python 3.11、FastAPI 和 LangGraph；MySQL保存用户、问诊、知识与治理数据，Ollama提供 qwen2.5:7b 聊天模型和 bge-m3 嵌入模型，FAISS保存医学知识分片索引。测试采用本地部署，服务通过回环地址通信，测试文本不会发送到第三方推理平台，主要配置见表5-1。",
    464: "测试按“单节点—业务链路—系统边界”三层展开。单节点覆盖危险信号匹配、否定表达、信息充分性、追问生成、科室投票、证据门控和加密；业务链路覆盖登录、问诊、记录、知识入库、索引激活和医生复核；系统边界则检查权限、超时、容量、断流、模型不可用及数据库异常。每条输入在运行前写入 task、gold_route、gold_department、gold_risk、gold_red_flags 和 acceptable_doc_ids 等固定字段。高风险以 gold_red_flags 非空为准，证据分诊要求症状充分且存在目标科室，低证据样本来自受支持科室之外，追问样本要求症状为空或仅有单症状且缺少持续时间和严重程度。P0—P4只表示扰动强度，不改变金标准；benchmark_1000.py 使用种子20260831生成后冻结。TRIPOD-LLM报告规范也要求交代任务、数据、评价过程和适用边界[22-23]。",
    465: "测试集包括高风险、证据支持、低证据和信息不足四类输入。固定种子20260831生成数据后，先冻结测试集，再逐条记录金标准、扰动等级和预期路径。高风险文本走生产安全筛查；证据支持和低证据文本使用 bge-m3、FAISS及证据加权分类；信息不足样本只调用结构化症状充分性和追问规则。自由文本回答不计入分类指标，以免随机生成质量干扰确定性组件的评测。",
    470: "危险信号召回率=正确识别的危险信号样本数/危险信号样本总数；非高风险特异度=正确未触发样本数/非高风险样本总数。",
    471: "科室和风险等级均按 Precision、Recall 与 F1 统计：",
    476: "其中 C 是标签数量。每个标签先单独计算 F1，再取算术平均，从而避免样本量较大的类别完全支配结果。",
    477: "信息不足样本不进入科室和风险 F1，而是单独计算追问触发率。证据支持样本用 Recall@3=命中可接受证据的样本数/证据支持样本数，并计算 MRR@3=(1/M)Σ(1/r_i)，其中 r_i 是第 i 条样本首个可接受证据在前三位中的名次，未命中记为0。科室指标同时报告四个受支持科室的 Macro-F1，以及加入全科/线下回退类别后的五分类 Macro-F1。组件耗时从函数开始执行计到返回，不包含并发队列、浏览器、业务后端、数据库和网络传输，所以不能等同于线上端到端延迟。",
    483: "高风险样本按八类危险表现构造：胸痛、呼吸困难、气促、咯血、便血、晕厥、意识不清和大出血，每类40条。每类再按 P0—P4 分成规范表达、口语与同义表达、组合与背景干扰、否定与跨症状干扰、错别字与词表外改写。证据支持样本对应心血管内科、呼吸内科、消化内科和皮肤科；低证据样本取自骨科、耳鼻喉、眼科、口腔和泌尿等非目标专科；信息不足样本只保留空症状或单症状，不给出持续时间和严重程度。",
    491: "安全筛查在普通模型调用之前运行。320条高风险样本中，P0—P3共256条全部命中，P4的64条错别字或词表外改写走了普通检索路径，总体召回率为80.00%。其余680条非高风险样本均未触发危险信号，工程测试特异度为100.00%。这说明规则对已登记词形、否定和转折表达的处理较稳定，分级统计见图5-1。",
    496: "信息充分性规则同时查看症状数量、持续时间和严重程度，再决定是否检索。160条信息不足样本全部进入追问，触发率为100.00%。这个结果只反映结构化症状对象上的确定性规则，范围限于信息充分性判断和追问节点，不代表 qwen2.5:7b 的自由抽取质量。",
    497: "追问测试说明，“没有危险信号”并不等于“信息已经足够”。空症状对象先询问主要不适；只有一个症状且没有持续时间和程度时，则优先询问持续时间或其他症状。该分支在固定结构化输入下阻止系统过早分诊，结果对应症状抽取完成后的规则层。",
    499: "320条证据支持样本都返回了至少一条索引证据，证据返回率为100.00%；但 Top-3 中含有预先标注可接受文档的比例只有71.88%，MRR@3为0.6255。也就是说，“有证据返回”和“返回了目标证据”是两个不同指标。之后仍按科室分数加权，回答层只渲染结构化分诊字段及通过治理检查的引用，见图5-2。",
    502: "图5-2汇总了320条受支持科室样本和200条低证据样本的五分类混淆矩阵。仅统计四个受支持科室时，准确率为95.94%，Macro-F1为0.9585；加入回退类别后，准确率降到59.04%，Macro-F1降到0.5861。200条知识库外症状全部被分到四个受支持科室，回退类别召回率为0，说明当前相似度阈值还没有形成有效的分布外拒答边界。",
    503: "回答编排测试关注失败关闭。证据缺少审核状态、HTTPS来源、发布日期、版本或许可信息时，不会进入引用列表；分诊字段超出允许枚举时，渲染器改用安全回退值。聊天模型生成的自由文本也不能覆盖最终分诊字段，因此不会在回答中自行加入处方、剂量或确定性诊断。",
    506: "图5-1的横坐标是五个扰动等级，纵坐标表示各路径的样本比例。八类危险信号各有40条样本，每类都在 P0—P3 命中32条、P4漏检8条，所以类别召回率和总体召回率均为0.8000。80%的一致结果来自测试集固定配置，并不能说明八类信号的实际难度完全相同。",
    507: "P4的64条输入包含“胸口像被压住一样疼”“神志模糊叫不醒”“伤口一直血流不止”等词表外说法。按当前路由，它们进入普通检索路径，说明危险信号规则的边界仍由登记词形和同义归一化结果决定。后续扩充词表时，还需配合语境约束和人工复核，以平衡召回与误报。",
    509: "去掉160条追问样本后，风险标签统计覆盖840条输入。低、中、高风险 F1 分别为0.0000、0.7080和0.8889，Macro-F1为0.5323，准确率为68.57%。低风险 F1为0，是因为200条非目标专科样本都走了候选证据路径；高风险差异主要来自64条 P4 词表外改写。图5-3比较了各路径的离线组件耗时。",
    512: "规则命中样本（256条）的 P50 和 P95 耗时分别为0.014 ms和0.020 ms；64条高危漏检样本进入检索后，P50和P95升至690.715 ms和913.475 ms。证据支持与低证据测试的 P50 分别为686.198 ms和670.048 ms，主动追问规则的 P50 为0.012 ms。图中使用对数坐标，数据只代表本机离线组件处理时间。",
    515: "检索测试覆盖空查询、索引缺失、低分过滤、Top-K截取、同义词扩展和活动版本切换。既有自动化测试验证了空查询与索引异常的失败关闭；千例评测暴露出阈值边界：200条非目标专科症状都拿到了高于当前阈值的候选证据，低证据回退率为0.00%。因此，这组输入被当前策略归入候选证据路径，结果只用于界定本轮工程测试的回退范围。",
    516: "活动索引由 active.json 指针切换。新版本先在临时目录生成向量和元数据，核对向量数与分片数后，再原子替换活动指针；检索器依据活动标记刷新进程缓存，并把新的 index_version 写入检索结果。这样可以避免版本切换留下半完成索引。",
    518: "引用测试逐项检查来源、分片、链接、日期、版本、许可和审核信息。320条证据支持样本都返回了结构化引用，但 Recall@3只有71.88%，说明元数据齐全并不能替代相关性判断。医疗 RAG 综述也把检索方法和评价框架视为尚待统一的问题[24]；本系统因此在记录详情页和 Trace 中保留来源与索引版本，评测文件另存每条样本的 Top-3 文档及排序分数。",
    519: "这里需要区分“有来源”和“来源可信”。知识库可以保留待审核、解析失败或尚未向量化的文档，但它们不能进入普通分诊。系统同时记录失败状态与审核状态，方便定位知识问题，也避免把命中数量直接当作医学回答质量。",
    525: "数据保护测试检查 JWT Cookie 的 HttpOnly 属性、CSRF 令牌、内部 AI 服务令牌隔离以及 AES-256-GCM 加密。加密服务要求32字节密钥，使用12字节随机 Nonce 和128位认证标签；密钥环测试确认旧密钥数据仍可读取，新写入数据使用活动密钥。密文被修改、密钥未知或长度过短时，系统抛出认证失败，不返回明文。",
    527: "异常测试覆盖 AI 服务不可用、聊天模型或嵌入模型缺失、知识索引损坏、请求过大、会话冲突、容量超限、推理超时、客户端取消和流式断开。首次事件前的临时网络错误只做有限重试，参数错误、冲突和请求过大不重试；流式中断会生成错误 Trace，客户端取消会释放推理租约，数据库持久化失败也不会盖住原始 AI 错误。",
    528: "健康检查分别列出 Ollama、聊天模型、嵌入模型、知识索引、模型治理和共享状态。任一关键组件不可用时，接口返回 degraded，前端显示降级提示；监控页从 LiveTraceRegistry 接收 failed、cancelled 和 completed 状态，避免异常请求长期停留在运行中。",
    531: "在限定为四个受支持科室的320条样本上，科室 Macro-F1为0.9585；呼吸内科 F1最高（0.9877），心血管内科最低（0.9189）。加入200条知识库外症状后，五分类 Macro-F1降至0.5861。前者考察封闭类别区分，后者考察开放输入下的回退，两项结果不能互相替代。",
    533: "风险 Macro-F1为0.5323，比旧测试中的近满分结果更能反映扩展扰动后的路径分布。高风险 F1为0.8889，对应 P4 词表外改写；中风险 F1为0.7080，与非目标专科样本进入候选证据路径有关；低风险 F1为0。由此可见，风险标签分布同时受规则词表和证据阈值影响，本组指标只适用于当前固定测试集和系统版本。",
    535: "本轮结果只在既定任务和评价口径内解释，不能把组件指标直接外推为医疗效果[70]。高风险优先规则与急诊分诊筛查研究所强调的时间敏感性相符[26]；互联网医院流程研究则把入口、信息提交和路径衔接视作连续服务过程[27]。相关综述分别讨论了医疗大语言模型的文本处理、问答、辅助决策[28]，基层部署的场景约束与责任[29]，以及 RAG 中检索、重排和生成的衔接[30]。",
    536: "从本轮结果出发，后续改进可先做四件事：扩充经过审核的危险信号同义归一化和语境识别，并用未登录表达持续回归；使用知识库外硬负样本重新标定检索阈值，或加入独立的分布外检测和拒答判据；检查心血管内科易混样本及 Recall@3 未命中样本的查询扩展、分片内容和排序权重；把症状抽取、知识版本、引用相关性和人工复核纳入端到端评测。",
    537: "已有研究从不同角度讨论医学问答中的证据组织、结果校验、多粒度检索、模型评价和人工参照方法[31-35]。这些工作共同提示，工程评测需要把模型放回具体任务和数据环境中考察，而不能只看一段生成文本。",
    538: "为单独观察否定感知和分句处理的作用，本文在同一固定种子的1000条输入上做了最小消融。完整方案保留 DANGER_SIGN_TERMS、同义词表、否定感知和分句逻辑；消融方案只做规范词与同义词的简单子串匹配，去掉否定和连接词继承。两种方案使用相同金标准和统计口径，结果见表5-4。",
    540: "完整方案与简单子串方案的召回率差异不大，但非高风险特异度相差10.59个百分点。原因主要在否定表达和连接词继承：消融方案会把“没有胸痛，也没有呼吸困难”中的词项直接算作命中，而完整方案先判断句子的断言状态。",
    543: "在相同的1000条工程测试集上，完整方案危险信号召回率为80.00%，消融方案为79.37%，差值为0.63个百分点；非高风险特异度则从89.41%升到100.00%，提高10.59个百分点。由此看，否定感知、分句和连接词继承主要减少普通样本的误触发，对危险信号召回率的帮助相对有限。",
    545: "本章以固定种子生成并执行1000条人工构造工程测试。危险信号召回率为80.00%，四个受支持科室的 Macro-F1为0.9585，加入回退类别后为0.5861；风险 Macro-F1为0.5323，Recall@3为71.88%，追问触发率为100.00%，低证据回退率为0.00%。在封闭科室范围内，当前 Demo 的分诊和结构化追问较稳定；面对 P4 词表外表达及非目标专科输入，系统仍分别走普通检索和候选证据路径。",
    550: "本文以 MedPilot 为对象，依次完成需求分析、总体设计、详细实现和工程测试，形成了一个可运行的系统 Demo。",
    551: "需求分析阶段把使用者分为普通用户、医生、知识编辑人员、审核人员、管理员和审计人员，并据此梳理智能问诊、主动追问、辅助分诊、健康档案、知识审核、医生复核、Trace监控和审计需求。分析结果显示，医疗健康咨询除了最终回答，还必须处理危险信号、信息缺失、知识来源、权限和记录保存。",
    552: "总体设计采用用户端、业务后端、AI服务、数据存储和治理管理分层。LangGraph在 AI 服务中安排安全筛查、症状抽取、信息充分性判断、主动追问、知识检索、辅助分诊和回答编排；业务后端处理认证、角色授权、会话所有权、事件累积、持久化和异常。数据库把用户、会话、问诊记录、Trace、健康档案、知识文档、临床复核和治理证据分开保存，避免 AI 原始结果、医生决定与知识生命周期相互覆盖。",
    553: "详细实现先由否定感知匹配器检查胸痛、呼吸困难、气促、咯血、便血、晕厥、意识不清和大出血，命中后进入高风险快速通道。未命中时，本地大语言模型把描述整理成结构化症状；信息不全则返回带缺失字段的追问；达到条件后由 bge-m3 和 FAISS 检索医学证据，分诊模块按证据所属科室加权。最终回答经过字段和证据校验的确定性渲染器生成，模型自由文本不能覆盖分诊字段。",
    554: "业务支撑部分包括问诊记录、健康档案、复诊任务、知识文档与索引版本管理、医生复核、模型和知识治理、Trace监控及审计日志。JWT、CSRF、AES-256-GCM、医疗关系和紧急授权共同划定访问边界；临床复核不会改写 AI 原始结果，医生的确认、修改、退回或急诊升级决定另存。",
    555: "测试阶段扩展了高风险、证据支持、低证据回退和信息不足追问样本，并检查安全筛查、检索、分诊、追问、权限、加密、断流、超时和索引异常等路径。危险信号召回率、科室 F1 和风险等级 F1 的统计图，用来描述当前 Demo 的工程行为和规则覆盖情况。",
    557: "后续工作可以分别从数据、模型、流程和工程推进。数据方面，脱敏研究提示敏感字段应和模型流程一起设计[36]，数据投毒研究提醒知识来源和输入环境也属于安全边界[37]，智能化语言服务研究则强调医疗文本交互的场景性[38]。结合本文实现，后续仍需维护词表、同义映射、知识来源和审核记录，并保证每次更新都能追溯。",
    558: "模型方面，可以比较不同本地聊天模型和嵌入模型在症状抽取、追问生成和检索排序上的差异。相关研究分别讨论了医疗智库中的知识服务流程[39]、国内医疗大语言模型的技术分布[40]、合成数据与模型输出的质量检查[41]、聊天机器人演进[42]以及研究热点变化[43]。因此，后续评价应同时覆盖信息完整性、危险信号召回、证据相关性、解释一致性和回答安全性，而不只比较文本相似度。",
    559: "流程方面，可把医生复核反馈接入规则和知识的回归分析，形成“异常 Trace—变更请求—验证证据—回滚结果”的闭环。伦理研究提醒，系统进入临终关怀等敏感服务时必须划清责任[44]；健康治理研究强调技术能力要和服务流程、治理机制协同[45]；人工智能伦理安全研究则要求责任、审计和风险处置落实为可执行环节[46]。如果未来接入医院流程，还要在取得机构授权后对接统一身份、患者主索引、就诊关系和院内科室编码，并重新确认数据访问范围。",
    560: "工程方面，后续可继续处理并发控制、缓存、索引切换、监控告警和备份恢复，并开展更大规模压力测试与故障演练。前端则可在保持现有状态边界的前提下，改善移动端问诊、引用查看和复核协同体验。",
    614: "我最想先感谢父母。大学期间，无论是学习还是生活，他们一直在背后支持我，给我面对困难的底气。论文和系统开发中的每一步，都离不开他们的理解、包容和付出。今后我会继续认真走好自己的路，用实际行动回应这份养育之恩。",
    615: "感谢李润东、宗科、荆梓恒学长，以及夏妍欢、沈欣怡、倪霜霜学姐。你们在专业学习、竞赛项目和生活经验上给过我很多具体帮助，我遇到问题时也总能得到耐心的建议和鼓励。祝大家毕业后的道路顺利，今后有机会再见面聊天。",
    616: "感谢同学刘坤和樊诗煜。我们一起上课、做项目，也在课后互相打气。那些讨论代码、赶进度和一起度过的日常，让大学生活多了许多值得记住的片段。祝你们今后的学习和工作都顺利，平安开心。",
    617: "感谢辅导员王群老师。大学期间，王老师在学习安排、生活管理和思想引导方面给了我们持续的帮助，也一直关注着我们的成长。谢谢老师认真负责的陪伴。",
    618: "感谢石玲老师。在专业学习和实践探索中，石老师用严谨的态度和耐心的指导帮助我拓宽视野，也让我更重视独立思考和解决问题的过程。",
    619: "最后特别感谢指导老师王伟老师和陈祥老师。从课题确定、方案论证，到系统实现和论文修改，两位老师都提出了具体而严格的意见。我正是在一次次修改和确认中完成了这份毕业设计。感谢两位老师的指导，祝老师们工作顺利、身体健康。",
}


REPLACEMENTS = [
    ("总体而言，", "综合本章内容，"),
    ("此外，", "同时，"),
    ("在上述分析基础上，", "基于前面的分析，"),
    ("通过梳理", "从梳理结果看，"),
    ("结果表明，", "从测试结果看，"),
    ("系统还实现了", "在此基础上，系统提供了"),
    ("系统采用", "实现中采用"),
    ("系统能够", "系统可以"),
    ("能够实现", "可以完成"),
    ("主要用于", "重点承担"),
    ("用于保存", "负责保存"),
    ("用于记录", "负责记录"),
    ("负责", "承担"),
    ("形成了", "得到"),
    ("形成", "得到"),
    ("便于", "方便"),
    ("具体字段结构及含义如", "字段和含义见"),
    ("字段结构如", "字段见"),
    ("如图", "图"),
]


def generic_rewrite(text: str, idx: int) -> str:
    """Apply conservative, meaning-preserving variation to any marked paragraph."""
    original = text
    if idx in MANUAL:
        return MANUAL[idx]
    if not text.strip():
        return text
    if text.startswith("表") and re.match(r"表\d+-\d+", text):
        text = text.replace("(", "（").replace(",", "，").replace(")", "）")
    for old, new in REPLACEMENTS:
        if old in text:
            text = text.replace(old, new, 1)
    # Break a few highly formulaic semicolon chains into more natural sentences.
    if text.count("；") >= 2 and len(text) > 120:
        parts = text.split("；")
        text = "。".join(p.rstrip("。") for p in parts)
        if not text.endswith("。"):
            text += "。"
    # Ensure a marked paragraph is materially changed even when no phrase matched.
    if text == original:
        if text.endswith("。"):
            text = text[:-1] + "。"
        else:
            text += "。"
    return text


def citation_tokens(paragraph):
    tokens = []
    for run in paragraph.runs:
        value = run.text.strip()
        if value and run.font.superscript and re.fullmatch(r"(?:\[\d+\]|\d+)", value):
            tokens.append(value)
    return tokens


def replace_paragraph(paragraph, new_text: str):
    old_runs = list(paragraph.runs)
    base = next((r for r in old_runs if r.text.strip() and not r.font.superscript), None)
    base_rpr = deepcopy(base._element.rPr) if base is not None and base._element.rPr is not None else None
    superscript_rpr = {}
    for r in old_runs:
        val = r.text.strip()
        if val and r.font.superscript and re.fullmatch(r"(?:\[\d+\]|\d+)", val):
            superscript_rpr[val] = deepcopy(r._element.rPr) if r._element.rPr is not None else None
    for r in old_runs:
        paragraph._p.remove(r._element)
    def apply_rpr(run, rpr):
        if rpr is None:
            return
        existing = run._element.rPr
        if existing is not None:
            run._element.remove(existing)
        run._element.insert(0, deepcopy(rpr))

    citation_set = set(superscript_rpr)
    pattern = r"(\[\d+\]|(?<![A-Za-z])\d+(?![A-Za-z]))"
    pos = 0
    for match in re.finditer(pattern, new_text):
        token = match.group(0)
        if token not in citation_set:
            continue
        if match.start() > pos:
            run = paragraph.add_run(new_text[pos:match.start()])
            apply_rpr(run, base_rpr)
        run = paragraph.add_run(token)
        rpr = superscript_rpr.get(token)
        apply_rpr(run, rpr)
        pos = match.end()
    if pos < len(new_text):
        run = paragraph.add_run(new_text[pos:])
        apply_rpr(run, base_rpr)
    if not paragraph.runs:
        paragraph.add_run(new_text)


def main():
    doc = Document(str(SOURCE))
    changed = []
    for idx in sorted(MARKED):
        if idx >= len(doc.paragraphs):
            continue
        paragraph = doc.paragraphs[idx]
        new_text = generic_rewrite(paragraph.text, idx)
        if new_text != paragraph.text:
            replace_paragraph(paragraph, new_text)
            changed.append(idx)
    OUTPUT.parent.mkdir(parents=True, exist_ok=True)
    doc.save(str(OUTPUT))
    print(f"saved={OUTPUT}")
    print(f"marked={len(MARKED)} changed={len(changed)}")
    print("changed_indices=" + ",".join(map(str, changed)))


if __name__ == "__main__":
    main()
